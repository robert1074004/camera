<div class="card text-center mx-auto my-5 border border-2 border-dark" id="category_panel">
    <div class="card-header">
        <h4>選擇預借日期</h4>
    </div>
    <div class="card-body">
        <div class="alert alert-warning alert-dismissible fade show" role="alert">
            預借日期需與今天相隔2天
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
        <h5 class="card-title">{{equipment.name}}</h5>
        <img src="{{equipment.image}}" width="120" length="120" alt="..." class="mx-auto">
        <form action="/equipments/show/{{equipment.id}}" method="POST">
            <p class="card-text my-2"> $NT {{equipment.price}} / 1天</p>
            <div class="form-group mb-3">
                <label for="storage">租借數量(目前庫存 : {{equipment.quantity}})</label>
                <input name="quantity" id="storage" type="number" max="{{equipment.quantity}}" min='1'
                    style="width:80px;height:30px;display:inline-block;" class="form-control ms-2" required>
            </div>
            <input type="date" name="date" class="mb-2" required>
            <button type="submit" class="btn btn-primary btn-sm">確定日期</button>
        </form>
    </div>
</div>